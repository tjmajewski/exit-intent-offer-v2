engine-strict=true
shamefully-hoist=true
# Lockfile predates strict peer checks: @shopify/shopify-app-session-storage-prisma@7
# peers prisma 6 while the app runs prisma 5 (fine at runtime). Without this,
# npm install / npm ci fail ERESOLVE on a lockfile that Docker builds fine.
legacy-peer-deps=true
